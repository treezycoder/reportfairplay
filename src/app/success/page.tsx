import SuccessPage from "@/components/templates/success";
import { Suspense } from "react";

export default function Success() {
  return (
    <Suspense>
      <SuccessPage />
    </Suspense>
  );
}
